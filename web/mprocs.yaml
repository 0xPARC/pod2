procs:
  server:
    cwd: ..
    shell: RUST_BACKTRACE=1 RUST_LOG=info cargo run  --bin pod2-server 
  ui:
    cwd: ./apps/playground-client
    shell: pnpm dev

  zukyc:
    autostart: false
    cwd: ./apps/pod2-issuer-client
    shell: pnpm dev

